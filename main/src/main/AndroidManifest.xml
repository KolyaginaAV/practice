<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Draft">
        <!-- android:exported="true" означает, что активность может быть запущена из других приложений -->
        <!-- <intent-filter>: означает, что эта активность — главная (MAIN) и должна отображаться в списке приложений -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Draft">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- здесь нет intent-filter, значит, она может быть запущена только явно изнутри приложения -->
        <activity android:name=".SecondActivity"/>
    </application>

</manifest>